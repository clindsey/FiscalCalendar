View = require "views/base/View"

module.exports = View.extend "{{#camelize}}{{name}}{{/camelize}}View",
    create: ->
      {{name}}View = @_super()

      {{name}}View
  ,
    dispose: ->
      @_super()
