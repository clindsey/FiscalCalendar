{{#camelize}}{{name}}{{/camelize}}Model = require "models/{{#camelize}}{{name}}{{/camelize}}"

describe "Model {{#camelize}}{{name}}{{/camelize}}", ->
  beforeEach ->
    @{{name}}Model = {{#camelize}}{{name}}{{/camelize}}Model.create()

  afterEach ->
    @{{name}}Model.dispose()

    expect({{#camelize}}{{name}}{{/camelize}}Model.getUsedLength()).to.equal 0
